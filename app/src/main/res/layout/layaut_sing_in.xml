<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@drawable/rider_splash_screen"
    android:layout_width="match_parent"
    android:padding="16dp"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:layout_margin="16dp"
        android:layout_centerHorizontal="true"
        android:padding="16dp">
        <Button
            android:id="@+id/btn_phone_sign_in"
            android:padding="16dp"
            android:layout_marginBottom="10dp"
            android:backgroundTint="@android:color/white"
            android:drawableStart="@drawable/ic_phone_black24dp"
            android:layout_width="match_parent"
            android:textAlignment="center"
            android:text="sigin in with phone"
            android:textColor="@android:color/black"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/btn_google_sign_in"
            android:padding="16dp"
            android:layout_marginBottom="10dp"
            android:backgroundTint="@android:color/white"
            android:drawableStart="@drawable/fui_ic_googleg_color_24dp"
            android:layout_width="match_parent"
            android:textAlignment="center"
            android:text="sigin in with Google"
            android:textColor="@android:color/black"
            android:layout_height="wrap_content"/>

    </LinearLayout>
</RelativeLayout>